# Backend API base URL.
# Leave empty to use the Vite dev proxy (same-origin /api -> http://127.0.0.1:8000).
# Set to an absolute URL (e.g. https://api.example.com) to call the backend directly.
VITE_API_BASE_URL=
